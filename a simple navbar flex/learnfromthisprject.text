1.always use padding in rem because its easy for responsiveness
2.🔎 -webkit-background-clip: text

Normally, background-clip controls how far a background color/image extends inside an element.

When set to text, the background is “clipped” to the shape of the text.

This is not standard across all browsers, so we add the -webkit- prefix (mostly for Chrome, Safari, Edge).

🔎 -webkit-text-fill-color: transparent

Normally, text has a fill color (like color: black;).

If we don’t make it transparent, the solid text color will hide the gradient background.

Setting it to transparent ensures the gradient shows through the text.